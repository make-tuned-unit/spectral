## Rebuttal
- R-08: CONFIRM — `import_pack` binds wire `obj.author_id` into `source_brain_id` with no `signature` column (`federation_sync.rs:296-299`, `:359-375`); module has zero `signature` mentions. `apply_tombstone_tx` hard-deletes `memories` by `id = target_hash` with only a wing-membership guard, no author/auth check (`:432-449`).
- R-09: CONFIRM — `sqlite_store.rs:277-282`, reader pool `:219-223`, `graph_store.rs:115-121`, and `recognition/.../store.rs:186-190` set no `busy_timeout`/`busy_handler`; production `crates/` grep finds none (only a test helper at `deletion_guarantees.rs:163`).
- R-10: CONFIRM — `GraphStore::open` opens with no PRAGMAs (`graph_store.rs:115-121`); default journal is DELETE. `vacuum`'s `wal_checkpoint(TRUNCATE)` at `:158` and `:162` returns a no-op on non-WAL (`(0,-1,-1)` verified), despite D4 docs at `brain.rs:2769-2771` relying on them for `graph.sqlite`.
- R-11: CONFIRM — `memory_signing_payload` signs only `source_brain_id`, `content_hash`, `created_at`, `visibility` (`identity.rs:223-244`); `verify_hit` hashes `hit.content` and never reads `hit.key` (`brain.rs:1218-1241`). Call sites are the public wrapper (`spectral/src/lib.rs:278-282`) and tests only — federation merge never invokes it.
- C3: WITHDRAW → NOT MET — Pitch/`README` promise “one SQLite file” (`docs/pitch.md:49`, `README.md:145`); `Brain::open` opens `graph.sqlite`, `memory.db`, and `recognition.db` (`brain.rs:995`, `:1002-1004`, `:1086`).
- C4: DEFEND PARTIAL — FTS5+`bm25()` exists (`sqlite_store.rs:495`, `:2160`), but primary recall is TACT fingerprint→wing→FTS plus cascade re-ranking (`brain.rs:2315-2320`), so “Recall = FTS5 + BM25” understates the live path.
- C8: DEFEND PARTIAL — `reinforce` and read-time `decayed_signal_score` exist (`brain.rs:3571-3591`, `:4741-4761`) and Archivist decay is opt-in, but the turn ledger explicitly refuses wiring unused evidence to auto-decay (`turn.rs:507-512`), so the pitch’s ambient “unused ones decay” loop is not fully met.
- C9: WITHDRAW → PARTIAL — Fan-out calls unscoped `recall_cascade` then post-filters (`federation.rs:384`, `:392-396`); that path is documented as returning every hit (`brain.rs:2335-2341`). This matches my own X-06/CU-06 P1: visibility after Private top-k truncation is not true pre-truncation scoping.
- C14: WITHDRAW → PARTIAL — Root `LICENSE` is Apache-2.0 and `Cargo.toml:21` declares it, but `NOTICE:2-4` is placeholder “Alice Doe” / “Polaris Media”, which Apache-2.0 §4(d) requires downstream redistributors to propagate.
