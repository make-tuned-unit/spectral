## Rebuttal
- **OP-03: CONFIRM** — `import_pack(store: &SqliteStore, pack: &Pack)` (`federation_sync.rs:273`) takes no key, peer identity, or authorization argument, and its tombstone loop at `:396-398` (`for t in &pack.tombstones { apply_tombstone_tx(&tx, &pack.wing_id, &t.target_hash)?; }`) reaches the hard-delete unconditionally; the only other caller, `apply_tombstone` (`:419-425`), is a bare transaction wrapper with no gate either. The module's own doc concedes the gap it leaves: `federation.rs:117` points at `Brain::verify_hit` as the intended remedy, and grep across all non-test code shows `verify_hit` has zero call sites outside the facade re-export at `spectral/src/lib.rs:278`.
- **OP-04: CONFIRM** — `grep -rn "busy_timeout|busy_handler|set_busy"` across `crates/` and `scripts/` returns exactly one hit, `crates/spectral-graph/tests/deletion_guarantees.rs:163`, which is a test-local raw connection and not library code. rusqlite 0.40.1 (`Cargo.lock`) installs no busy handler by default — `Connection::open` is a thin `sqlite3_open_v2` wrapper — and the reader pool does not differ, since `open_reader_pool` (`sqlite_store.rs:214-225`) sets only `temp_store`, `mmap_size`, and `query_only`; readers matter less anyway, as WAL readers do not block, but `graph.sqlite` has no WAL (see OP-05).
- **OP-05: CONFIRM** — the persistence counterargument is the right one to test, and it fails: `grep -rn "journal_mode" crates/ scripts/` returns exactly four hits, and none touch `graph.sqlite` — `sqlite_store.rs:278` (memory.db), `store.rs:187` (recognition.db), and two bench binaries that WAL their own throwaway files (`stmt_cache_probe.rs:15` opens `tmp/probe.db`, `batch_write_headroom.rs` builds a private temp schema). `graph.sqlite` is created and opened only at `brain.rs:995-999` via `GraphStore::open`/`open_read_only`, neither of which issues any PRAGMA, and the archivist never touches it (`archivist/src/main.rs:41` resolves to `memory.db`), so no real file can have acquired WAL — meaning the two `PRAGMA wal_checkpoint(TRUNCATE)` calls at `graph_store.rs:158` and `:162` are no-ops on every brain in existence.
- **OP-07: CONFIRM** — `content_hash` is computed as `blake3::hash(content.as_bytes())` at `sqlite_store.rs:1683-1685` and again at `:3995`, over content bytes alone with no key, id, or salt mixed in. `verify_hit` (`brain.rs:1231`) recomputes it the same way from `hit.content`, and `memory_signing_payload` (`identity.rs:239`) length-prefixes only `content_hash`, `created_at`, and `visibility` — so nothing binds the signature to the key under which the row is served.
- **C3: WITHDRAW → PARTIAL** — my own verdict text already conceded the embedded/no-service half, so "NOT MET" was the wrong label for a three-part conjunctive claim where two parts hold. The file-count evidence is unchanged and I retain it: `graph.sqlite`, `memory.db`, and `recognition.db` at `brain.rs:995`, `:1004`, `:1086`, against `README.md:146`'s "a single SQLite file you own"; OP-06 (no cross-store atomicity) is unaffected by the relabel.
- **C4: WITHDRAW → PARTIAL** — the other reviewers are right and I checked the wrong entry point: `cascade_retrieve_scoped` (`brain.rs:2043`) calls `tact_retrieve_with_k_scoped` **first** and only backfills raw FTS when `hits.len() < k` (`:2044-2058`), so the cascade path is fingerprint → wing → FTS, not an FTS5+BM25 equality. The crate's own doc at `spectral/src/lib.rs:88` says the fingerprint/wing tiers "are measured as adding nothing over FTS" — measured-as-neutral is not the same as absent, and they still execute and still shape the pool that reranking sees.
- **C8: WITHDRAW → PARTIAL** — the decisive check is call sites: `decayed_signal_score` has exactly one, `brain.rs:1936`, on the `recall`/`recall_at`/`recall_local` path only, so the canonical `recall_with`/`recall_cascade`/`recall_topk_fts` paths apply no signal decay at all. Combined with `turn.rs:507-512` explicitly refusing to wire non-use to decay ("Wiring this to automatic decay or forgetting would let the write path erase evidence of a read-path defect"), the strengthen half is default library behavior but the decay half is neither outcome-driven nor present on the recommended path.
- **C9: DEFEND (PARTIAL)** — beyond OP-01, "provenance-ranked" is weaker than it sounds: `merge_and_rank` (`federation.rs:417-420`) takes `Vec<(BrainId, f64, Vec<MemoryHit>)>` and ranks on the caller-supplied `weight` plus RRF over within-child rank, never verifying a signature. `verify_hit` — the mechanism `federation.rs:96` and `:117` both name as what makes provenance trustworthy — is called by nothing in the ranking path or anywhere else outside tests, so the "provenance" being ranked is self-asserted origin metadata.
- **C14: DEFEND (PARTIAL)** — `NOTICE:2-4` reads verbatim `Copyright 2026 Alice Doe`, blank, `This product includes software developed at Polaris Media.` Both names appear on the repo's own list of retired demo fixtures at `brain.rs:4304-4306` (`alice`, `apollo`, `acme`, `charity`, `vega`, `travel`, `polaris`, `infra`), which is decisive: the codebase itself classifies "alice" and "polaris" as fixture identifiers, so this is scaffolding left in the one file Apache-2.0 §4(d) obliges every downstream consumer to propagate.
