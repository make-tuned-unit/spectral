## Rebuttal
- R-08: CONFIRM — `MemoryObject` and `Tombstone` carry caller-supplied `author_id` but no signature (`crates/spectral-ingest/src/federation_sync.rs:27-43`); import persists that ID without verification (`crates/spectral-ingest/src/federation_sync.rs:296-299,358-379`), while imported tombstones unconditionally reach a hash-based hard delete (`crates/spectral-ingest/src/federation_sync.rs:393-398,432-453`).
- R-09: CONFIRM — the memory connection’s PRAGMA batch has no busy timeout (`crates/spectral-ingest/src/sqlite_store.rs:277-282`), nor do the graph open (`crates/spectral-graph/src/graph_store.rs:115-121`) or recognition PRAGMAs (`crates/spectral-recognition/src/store.rs:186-190`); the only repository `busy_timeout` call is test-only (`crates/spectral-graph/tests/deletion_guarantees.rs:163`).
- R-10: CONFIRM — `GraphStore::open` performs only `Connection::open` and schema creation (`crates/spectral-graph/src/graph_store.rs:115-121`), yet `vacuum` issues WAL checkpoints before and after `VACUUM` (`crates/spectral-graph/src/graph_store.rs:153-162`) despite never enabling WAL.
- R-11: CONFIRM — the signed payload contains brain ID, content hash, timestamp, and visibility but not the memory key (`crates/spectral-core/src/identity.rs:223-243`), and `verify_hit` validates those same fields without reading `hit.key` (`crates/spectral-graph/src/brain.rs:1218-1242`); repository callers outside the wrapper are tests (`crates/spectral-graph/tests/brain_tests.rs:1251-1277`).
- C3: WITHDRAW → PARTIAL — one public `Brain` handle owns the stores (`crates/spectral/src/lib.rs:235-255`), but construction opens separate `graph.sqlite`, `memory.db`, and `recognition.db` files (`crates/spectral-graph/src/brain.rs:995-1014,1082-1095`).
- C4: DEFEND — FTS5 and BM25 exist (`crates/spectral-ingest/src/sqlite_store.rs:495-498, crates/spectral-ingest/src/sqlite_store.rs:2153-2161`), but canonical recall invokes the cascade (`crates/spectral/src/lib.rs:445-468`), whose candidate entry point is explicitly fingerprint → wing → FTS (`crates/spectral-graph/src/cascade_layers.rs:378-405`); therefore “Recall = FTS5 + BM25” describes only part of the pipeline.
- C8: WITHDRAW → PARTIAL — unused-delivery tracking is explicitly evidence rather than automatic decay policy (`crates/spectral/src/turn.rs:495-511`), while mutating decay occurs only through opt-in Archivist `apply_decay` (`crates/spectral-archivist/src/decay.rs:18-30,46-67`).
- C9: DEFEND — cascade first retrieves an already capped `fetch_k` candidate set (`crates/spectral-graph/src/cascade_layers.rs:394-406`), and TACT plus its FTS supplement stop at `k` before visibility is applied (`crates/spectral-graph/src/brain.rs:2043-2053`); only afterward does the cascade retain visible candidates (`crates/spectral-graph/src/cascade_layers.rs:408-413`), so filtered-out hits can consume candidate slots.
- C14: WITHDRAW → PARTIAL — `NOTICE` contains apparent placeholder attributions, “Copyright 2026 Alice Doe” and “Polaris Media” (`NOTICE:2-4`), while Apache-2.0 §4(d) requires downstream derivative distributions to reproduce applicable NOTICE attributions (`LICENSE:107-116`).