# The measured record — what we tried, what held, what we rejected

Every retrieval or accuracy claim in this repo traces to a measured experiment,
and the negative results are load-bearing: they are why the shipped stack looks
the way it does. One line per experiment; the linked doc carries the full
method, numbers, and caveats. Nothing below is asserted without a run behind it.

## Held (shipped or validated)

| Experiment | Verdict | Date | Record |
|---|---|---|---|
| **Deep failure analysis — where the lift is (and why every lexical lever failed)** | **$0, from existing output. Three findings that reframe the retrieval programme.** (1) **Retrieval is worth +23.34pp and it is now priced**: `P(correct \| full evidence) = 88.31%` against 64.97% overall, with the residual 11.69pp belonging to the reader/answer-key/judge, not us. Zero-evidence questions score 21.29%, partial 35.36%, full 88.31%. Multi-session carries **+37.5pp** of that. (2) **~+9.4pp is reachable by ranking alone** — 66% of zero-evidence failures have their evidence in the match set (34% are true vocabulary misses, the lexical floor) — but **not via k**: the first evidence turn sits at **median rank 99**, so k=200 recovers 83% at ~5x tokens, which is why the 2026-07-20 k-rejection survived re-testing. (3) **Why six levers produced six nulls, in one fact**: the missed evidence has **HALF the query-term overlap (0.46x) and half the IDF-weighted overlap (0.47x)** of the distractors that outranked it. BM25 is ranking correctly by its own criterion; lexical overlap is simply a poor proxy for answer-bearing-ness here. So it is the **LEXICAL** lever family that is exhausted, not the retrieval one. The single feature that inverts is **first-person declarative (1.42x)** — orthogonal to lexical overlap, already implemented, and defaulting to **false on the topk_fts path the baseline ran**. Enabling it: **+3 evidence turns**. **That shortfall is the real finding** — see the composition row. | 2026-08-08 | [analysis](internal/failure-analysis-2026-08-08.md) |
| **Additive boosts cannot promote deep evidence — the composition is the ceiling** | **ARCHITECTURAL, measured arithmetically.** The composite score starts as FTS **rank position** (`1 - i/n`, `ranking.rs:345-347`) and every signal is an **additive** boost on it, so a boost's value *in ranks* depends on pool size. At the shipped `fetch_mult=3` (pool 120) adjacent ranks differ by 0.0083 and promoting the median recoverable evidence turn (rank 99 → top 40) needs **Δ=0.492**; recency (0.10) moves 12 ranks, declarative (0.10) 12, entity (0.05) 6, proximity (0.15) 18 — **all four combined, all maximal, all aligned, move 48. The requirement is 59.** The architecture cannot make the promotion even in an impossible best case. Widening does not rescue it: at pool 480 the same budget moves 192 ranks, but G4 measured recall *degrading* (72.7% → 61.5%) as weight rose, because the boosts then override BM25 wholesale. **Small pool: too weak. Large pool: destroys the ordering it sits on.** Root cause: rank position is a scale-free-less base score. **Proposed fix, untested and highest-value: reciprocal rank fusion, which this codebase ALREADY implements** (`sqlite_store.rs::ranked_ids`, used to fuse FTS channels) — fuse a BM25 ranking with per-signal rankings instead of adding boosts. Every lever refuted to date was refuted *inside* the composition this replaces, so those nulls do not carry over. | 2026-08-08 | [analysis](internal/failure-analysis-2026-08-08.md) |
| **Relative date offsets in rendered context** | **NOT VALIDATED — passed on one corpus, FAILED replication on a second. Default stays off.** Replication (LoCoMo `temporal-reasoning`, n=317, identical design, direction and gate fixed in advance): **72.56% → 72.24%, −0.32pp, 9 fixed vs 10 broken, McNemar exact p = 1.0000**, at **+8.6% context tokens** (2,728 → 2,963) — on this corpus it charges more and returns nothing. The primary p-value is not retracted; what is established is that the effect **does not generalise**, so the primary's scope is LongMemEval temporal-reasoning specifically, not "temporal questions". Untested hypothesis for the gap: the corpora differ ~5x in context size (14,026 vs 2,728 tokens) and the arithmetic failure the lever removes needs a large, temporally spread context to occur at all — testable, not concluded. **Primary result follows.** **PASS against a preregistered NULL.** LongMemEval `temporal-reasoning`, n=133 paired, single variable `SPECTRAL_DATED_CONTEXT=1`, expansion off both arms, temp 0, post-R21 scorer: **76.69% → 83.46%, +6.77pp**, contingency **9 fixed / 0 broken**, **McNemar exact p = 0.0039**, at **+1.1% context tokens** (14,026 → 14,184) and zero failures of any kind. The prereg committed to expecting null-or-negative and gave three reasons; two were wrong and the third was right about the mechanism and backwards about the conclusion — *"only a computation the model may have been getting wrong"*, and **the model was getting it wrong**. Mechanism measured, not inferred: **7 of the 9 fixed questions (78%)** are phrased in relative time vs 56% of the slice, and the fixed answers show the actor picking the wrong session when it did the date arithmetic itself (`2023-04-15` vs B's `2023-06-15 (7 days ago)`). **The lever deletes an arithmetic failure mode rather than adding information**, which also explains the zero regressions. **Replication INCOMPLETE**: LoCoMo n=317, arm A finished (72.56%) and arm B stopped at 59/317 when the API credit ran out. **The 57-question partial was deliberately NOT analysed** — reading it after knowing the primary is a garden-of-forking-paths, and availability is not a reason to spend it. Second preregistered accuracy win after R11 and on the same axis, but R11 was believed because it survived a disjoint validation set; this has not. Resume cost ~\$4. | 2026-08-08 | [prereg](internal/relative-offsets-prereg-2026-08-08.md), [result](internal/relative-offsets-result-2026-08-08.md) |
| **ForgetEval canonicalization prediction (tested against us)** | **PARTIALLY REFUTED IN OUR FAVOUR, and the thesis boundary is now drawn precisely.** $0 — no harness run, no dataset downloaded; what was tested is the benchmark's falsifiable *hypothesis* about deterministic stores. ForgetEval (arXiv 2606.15903, MIT, deterministic substring scoring, no LLM judge) reports deterministic primitives at **5% identifier-obfuscation / 0% cross-lingual**, recoverable only inscribe-time. Measured on Spectral: **cross-lingual reproduces 0% exactly** (fr/es/de/pt), but identifier obfuscation is **better than predicted** — `sk_…`, `SK-…` and `sk-abc…` all retrieve, because FTS5 `unicode61` case-folds and treats both `-` and `_` as separators; the gap appears only when the token **boundary** is removed (`skABC123XYZ`). Our first test asserted separator failure and was **wrong**; corrected to measured behaviour. Crucially, `the_canonicalization_gap_is_admission_not_ranking` shows k=500 with a 12x pool still cannot admit the cross-lingual match — **no ranking signal can reach it**, the same admission-vs-ranking split that decided G4, and why the effective fix is inscribe-time. This draws the zero-inference boundary: lexical/temporal/identifier-up-to-token-boundaries are *inside*; cross-lingual and open canonicalization are *outside*, and closing them needs write-time inference — a genuine trade to state wherever the claim appears. Untested mitigation: the shipped `query_aliases` file is a deterministic consumer-supplied canonicalization channel. **Running the real benchmark is the best remaining external move** — deterministic scoring removes the judge that costs our LoCoMo number 25.6pt of cross-harness spread, and it measures *forgetting*, where we hold verified deletion assets — but the published prior says a deterministic store lands ~**63.4%** vs 91.7-93.2% for mutation-time-hook configs, and that expectation must be preregistered BEFORE running. | 2026-08-08 | [assessment](internal/forgeteval-assessment-2026-08-08.md) |
| **Bi-temporal validity + wiring the temporal resolver (G1+G2)** | **SHIPPED FOR AUDITABILITY — no accuracy claim, and none is possible.** `memories` carried only system time (`created_at`/`updated_at`); `resolve_relative_dates` shipped exported with 11 tests and **wired to nothing**. Those are the same fact: the resolver had no honest sink, because writing a resolved world-date into `created_at` would lie about when we learned it. Added `valid_from` + `valid_to` with a far-future **sentinel** (NULL would force `IS NULL OR ...`, which cannot use an index) and half-open `[from, to)` so adjacent versions never both match. `invalidate_at` **never deletes**; `keys_as_of` reads `COALESCE(valid_from, created_at)`. **No benchmark in the field tests bi-temporal modelling** — which is why Zep's claim on it is unfalsifiable, and we claim no better position. What is asserted is a *property*, by test: a fact invalidated 2026-06-01 stays visible to a May query and invisible to a July one, stably, with unrelated facts unaffected — the property that makes an eval against a mutating store meaningful at all. **Two findings kept:** a backfill `UPDATE` during migration fires FTS sync triggers and fails on exactly the legacy databases the migration exists to upgrade (caught by an existing idempotency test; fixed by resolving at read time instead); and the first test premise was wrong rather than the code — a memory ingested today genuinely was not in the brain last May, and loosening the predicate to make the test pass would have destroyed the property. 10 tests, $0. **NOT done:** `as_of` through `Brain` (needs trait-level methods), resolver-at-ingest (default write-path change, needs prereg), and `RenderOptions::relative_offsets` accuracy (needs a paid A/B — the highest-value untested rendering lever, on the axis R11's +14.2pp came from). | 2026-08-08 | [result](internal/g1-g2-bitemporal-2026-08-08.md) |
| **BM25-only LoCoMo baseline (preregistered floor)** | **CORRECTED 2026-08-08 — the 65.02% stands; the retrieval interpretation was WRONG.** R19 made evidence-turn recall computable on LoCoMo: it is **59.86% micro / 68.63% macro**, not the 95.06% *session* recall this row originally cited, and **24.86% of questions retrieve ZERO evidence turns** (not 1.11%). Correct-vs-incorrect separation is **57.08pp** (88.62% vs 31.54%), not 5.94pp. **Retrieval IS the binding constraint on LoCoMo** — the original claim was the opposite, and it was reasoned from a metric R15 had already shown is saturated by construction. Dilution 20.6x. See `internal/r19-locomo-turn-labels-2026-08-08.md`. Original entry follows. **PUBLISHED AS PREREGISTERED — a floor measurement, not a competitive claim.** A memory layer with **zero model inference at read or write time** (`topk_fts`, `--no-expand-queries`) scores **65.02% (935/1438)** on the full LoCoMo answerable set, **95% CI [62.79%, 67.25%]** — cluster-robust over the 10 conversations with a t(9) small-G correction, *not* the naive binomial (±2.46pp) and *not* the vendors' ±0.31pp. **The result is the gap, not the number:** the same $0/~1 ms layer achieves **95.06% session recall (micro) / 97.13% macro**, retrieves ≥1 evidence session for 98.9% of questions, and mean session recall differs by only **5.94pp** between answers judged correct (99.21%) and incorrect (93.26%) — so **retrieval is not the binding constraint on this benchmark**. Per-category: single-session-user 70.15% (n=841), temporal-reasoning 73.82% (n=317), multi-session 39.64% (n=280, and the weak slice on both axes at 89.48% session recall). Cost **$17.38** ($0.01208/question) against a preregistered $18.26 — within 5%; retrieval itself is 1.03 ms mean and $0, so the entire spend is the reader. Hygiene: 0 transport, 0 auth, 0 zero-retrieval, 0 empty/error predictions, **4 judge-parse failures** (scored as wrong; 3 carried the judge's own `"correct": true` — opened as R21, worth 0.2pp, deliberately not fixed before publishing). **Two disclosed deviations:** the prereg anticipated ~±5.5pp clustered and we measured ±2.23pp (narrower — the more flattering result, so it is stated rather than absorbed; the ~±5.5pp figure was literature-derived, never measured on our data), and the first launch was killed after 7 questions to check the binary's `--confirm-cost` estimator, which printed **$115.04** from a flat `$0.04/call` constant (`eval.rs:97`), ~6.5× conservative. Same config, nothing tuned. **Not comparable to any vendor figure** (25.6pt cross-harness spread), against a ~93.6% practical ceiling (6.4%-wrong answer key), with a judge that accepts 62.8% of wrong-but-adjacent answers. | 2026-08-07 | [prereg](internal/bm25-locomo-baseline-prereg-2026-08-07.md), [result](internal/bm25-locomo-baseline-result-2026-08-07.md) |
| **Oracle evidence-turn metric (R15)** | **INSTRUMENT CORRECTION — no accuracy claim, no gate.** The oracle's `answer_keys_*` counted every turn of every `answer_`-prefixed session (10,960 turns) instead of the 896 turns LongMemEval labels `has_answer: true` — a **12.2× dilution**, so "key-recall" was evidence-SESSION turn coverage, never evidence recall. Fields renamed to `answer_session_turns_*` (serde aliases keep the whole archive loadable) and a first-class `evidence_turns_{total,retrieved}` added, computed through the same `ingest::memory_key` the write path uses. Backfilling the shipped-config run (`r12-baseline`) with the new `oracle-evidence` subcommand, $0 and offline: **evidence-turn recall 793/896 = 88.5% micro, 90.5% macro over 479 labelled questions, 27 retrieving zero evidence** — the same retrieval against a denominator 12.2× smaller, *not* an improvement on any previously published figure. The metric is **undefined, not zero**, on the 21 `_abs` questions and on every LoCoMo-converted set. Actionable finding: `single-session-preference` at **29/44 = 65.9%** with 9/30 zero-evidence. | 2026-08-07 | [note](internal/turn-level-evidence-recall-2026-08-07.md), [result](internal/r15-evidence-metric-2026-08-07.md) |
| **Dated session-grouped context (R11)** | **PASS, SHIPPED** — two-stage held-out LoCoMo A/B with byte-identical retrieval (identity-gated; first attempt voided itself, exposing R14 expansion nondeterminism): `session_grouped` vs the undated shipped block, dev **+19.2pp** (p=1.6e-6), disjoint validation **+14.2pp** (B-fixed 20/broke 3, McNemar p=4.9e-4). Entire effect is temporal-reasoning (20.0%→62.5% validation); other categories bit-flat — undated context starves temporal questions of dates. Facade recall surfaces now publish `session_grouped` as `context_block` (BREAKING for old-block parsers; pinned by test). First prereg-validated accuracy lever in the project's history. | 2026-08-06 | [prereg](internal/r11-render-ab-prereg-2026-08-05.md), [void](internal/r11-render-ab-stage1-void-2026-08-05.md), [stage 1](internal/r11-render-ab-stage1-result-2026-08-05.md), [stage 2](internal/r11-render-ab-stage2-result-2026-08-06.md) |
| **SQL tiebreak on the default FTS path (R16)** | **MERGED 2026-08-07 (#254) — determinism only, NO accuracy claim.** *(Status history: the implementation reported a green suite; `deterministic_anchor.rs:83 recency_decay_is_order_invariant_in_the_topk_path` in fact failed with the change present, which quarantined the branch. **Resolved by experiment: the tiebreak EXPOSES a pre-existing violation, it does not introduce one.** On `main` with no SQL change present, the same 24 memories with the same timestamps inserted shuffled instead of chronologically reorder across a 5-year clock shift. The old fixture inserted chronologically, which — untiebroken — made FTS rank order agree with age order, the one arrangement in which an additive freshness term cannot reorder anything; the property was never held and was never tested. Re-baselined under R20's Rule 5 interim requirement into `topk_additive_recency_reorders_under_a_clock_shift` (asserts the drift with `assert_ne!`, so any future R20 fix goes red) and `topk_ranking_is_independent_of_insertion_order` (two brains, same content, different write order, identical ranking — the property R16 buys). Both fail on the pre-R16 SQL. **R20 remains open and still needs its prereg.** See `internal/research-alignment-2026-08-07.md` §2, `REPAIR_REGISTER.md` R16/R20.)* `, m.id` added to the non-fusion `fts_search` ORDER BY and to both fusion channel subqueries (whose rank positions feed RRF), so the `LIMIT` boundary among equally-scored documents is decided by our SQL rather than by SQLite's query plan. `m.id` is `key_to_id(key)` — a pure function of the memory key, so the order reproduces across independently-built brains. Paired $0 oracle on the merge commit (pre-arm reproduced `r12-baseline.jsonl` at 0/500 and itself at 0/500): **10/500 contexts changed (2.0%)**, all `TopkFts`, **0/333 Cascade**; **9 reorder-only** (identical retrieved sets), **1 (`b9cfe692`) swaps one document**. Nothing moved: session-recall **98.03% macro / 97.78% micro**, answer-session turn coverage **55.51% macro / 53.02% micro** (the ~12x-diluted legacy metric, published as "key-recall" until R15; macro = mean of per-question ratios; micro = pooled), zero-answer-session-turn 2, mean tokens 14212.8, and evidence-turn recall (R15's `has_answer`) **793/896 = 88.50% micro / 90.54% macro with zero per-question change** — all identical in both arms. Cost ~0.4 ms p50 (5.06→5.44 ms, measured under concurrent load, directional only). The related `ORDER BY rank` "pure latency win" is **REJECTED**: it is mutually exclusive with the tiebreak and moves the boundary into FTS5's undocumented internal ordering (Rule 3). Not a general figure — measured on LongMemEval brains only. | 2026-08-07 | [record](internal/r16-baseline-shift-2026-08-07.md) |
| **Porter stemming (FTS5 tokenizer)** | **PASS** — zero-*coverage* questions 8→4 (the R15-diluted metric; rescored on evidence turns, `oracle-evidence` $0: evidence-turn recall 749/896 → 783/896, 5 zero-evidence questions fixed vs 4 introduced — directional, no gate), session recall 96.9%→97.6% at $0 (Tier 0); porter-only 46/60 vs LLM-expansion-only 43/60 in the paid head-to-head — a fully deterministic pipeline matches the LLM-assisted config, so porter replaces expansion at $0. Shipped as the default tokenizer. | 2026-07-02 | [ORACLE_TIER0](internal/ORACLE_TIER0.md), [TIER1_RESULTS](internal/TIER1_RESULTS.md) |
| **Identity-keyed counting prompt** | **PASS** — +8.0pp on held-out multi-session counting (18/25 → 20/25), zero regressions; the patterns (identity-keyed dedup, strict inclusion) generalize beyond the tuning set. | 2026-07-12 | [actor-counting-intervention](internal/actor-counting-intervention-2026-07-12.md) |
| **porter + fetch-pool widening (mult=3)** | **NEUTRAL, kept for cost** — accuracy-neutral vs baseline (23/39 both arms); of the 16 remaining failures, 15 had the answer retrieved — the actor, not retrieval, is the ceiling. | 2026-07-03 | [TIER1_PORTER_WIDEN](internal/TIER1_PORTER_WIDEN.md) |

| **Constellation fingerprint retirement** | **COST CONFIRMED, CONCLUSION WITHDRAWN** — the 7x/14.7x figures stand, but tier 1 fires on only 3.2% of questions *because its wing classifier is demo data*. Retiring a feature starved of its input is amputation; the real experiment is to build the taxonomy and re-measure. Default stays `true`. Measured cost: ~39% of write, ~57% of store bytes, 7.0–7.8x ingest and 14.7x storage, byte-identical retrieval over 361 questions. Exposed as `IngestConfig::fingerprints` for consumers who do not use wings, and as the control arm for the taxonomy experiment. | 2026-08-03 | [record](internal/fingerprint-retirement-2026-08-03.md) |
| **Tier-1 ungating (fire on wing alone)** | **REJECTED as preregistered** — reachability 0.0% → 12.0%, against a ≥30% gate; latency +15.4%/+21.9% across runs, straddling the +20% line. The mechanism works (once a wing is detected the index is never empty and the tier fires 26/27), but **the binding constraint is wing detection on the query: 12.4%**, not the hall gate. My ≥30% prediction came from a loose token proxy that overestimated the real classifier by ~3.7x. Default stays gated. | 2026-08-03 | [prereg](internal/tier1-ungating-prereg-2026-08-03.md), [result](internal/tier1-ungating-result-2026-08-03.md) |
| **Wings are a general retrieval accelerator** | **REFRAMED** — a wing fires when the query *names the project*, which real agent queries do 12.4% of the time ("Give me a tour of the app"). Wings are a **scoping mechanism for when scope is stated**, not a primary route. The other 87.6% belongs to ambient context (`RecognitionContext::focus_wing`), which is unexercised. | 2026-08-03 | [result](internal/tier1-ungating-result-2026-08-03.md) |
| **TACT tier-1 gate requires hall on the QUERY** | **DESIGN DEFECT — confirmed, and it is real but not sufficient.** On 217 real Permagent queries against the real taxonomy: wing fires **46.5%**, hall fires **5.5%**, both **0.9%**. Hall is a property of the *memory*, not the question — real queries ("Give me a tour of the app") don't announce what kind of memory answers them. Ungating tier 1 from hall would take reachability 0.9% → 46.5%. The existing 0-wins/2-losses/9-ties verdict was measured on 11 cases from a biased 3% slice and is **not** evidence about the design. | 2026-08-03 | [record](internal/wing-taxonomy-2026-08-03.md) |
| **Wings are auto-derivable from corpus statistics** | **REFUTED** — salient-term anchors trade coverage against discrimination with no workable point: permissive bands yield `just`/`want`/`could` (59.8% coverage, zero topicality), strict bands yield real topics that appear in almost no questions (11.0%). Wings are deployment knowledge; the real brain shows they work when a consumer supplies them (46.5% of real queries name one). | 2026-08-03 | [record](internal/wing-taxonomy-2026-08-03.md) |
| **Removing the demo wing fixtures** | **SIDE-OBSERVATION, not a claim** — held-out LoCoMo after removal: session-recall 92.9%→93.1%, zero-recall 4→3, answer-session turn coverage 13.8%→13.7% (**not** evidence recall — R15; LoCoMo carries no per-turn labels, so evidence recall is *unavailable* here, not zero), tokens flat. 39/120 contexts changed because the fixtures were matching `alice`/`acme` keywords on 8.3% of that corpus. Not preregistered and n=120, so directional only — but the fixtures were demonstrably not helping. | 2026-08-03 | [record](internal/wing-taxonomy-2026-08-03.md) |
| **Default wing rules are demo fixtures** | **DEFECT — REMOVED 2026-08-03** — `default_wing_rule_pairs()` ships `alice\|coffee\|noah\|carol-doe`, `acme\|widget\|bob\|recipe`, `apollo\|polymarket` as the library default. Worse, they capture live content in the **real** Permagent brain — `apollo` 46, `alice` 18, `acme` 17, `polaris` 16 memories filed into fictional topic areas by keyword collision, alongside the consumer's genuine wings. Not fixed — needs a migration plan. | 2026-08-03 | [record](internal/fingerprint-retirement-2026-08-03.md) |

## Rejected or refuted

| Experiment | Verdict | Date | Record |
|---|---|---|---|
| **K=60→80 admission widening, RE-TESTED on the corrected metric** | **THE ORIGINAL REJECTION SURVIVES — and that is worth knowing after R19.** The 2026-07-20 rejection was scored on answer-*session* recall, the metric R19 later showed is saturated by construction, so it was a candidate to be overturned the way the LoCoMo baseline's claim was. Re-run at $0 on the 500 archived brains, scored on R15/R19 evidence-turn recall: k=40 → k=80 moves **793/896 → 802/896 (+9 evidence turns)**, zero-evidence 27 → 25, for **+33% context tokens** (14,213 → 18,892). Same verdict as 2026-07-20, now on the right denominator: a real but tiny admission gain at a large token cost. **Not every prior null was an artifact of the diluted metric** — R19 weakened that class of evidence, it did not void it, and this is the case that shows the difference. Side observation: `single-session-preference` is **bit-identical** (29/44, 9 zero) because it routes to **cascade**, whose k is not controlled by `--max-results`; the earlier preference k-sweep moved only because it forced `topk_fts`. Any future preference-k lever must act on the cascade config, not the output cap. | 2026-08-08 | [original](internal/k-admission-test-2026-07-20.md), [R19](internal/r19-locomo-turn-labels-2026-08-08.md) |
| **Bounded typed memory units, read path (G5)** | **PREMISE REFUTED at equal budget — $0, and the strongest positive evidence the retrieval layer earns its place.** G5 claims retrieval-as-a-problem *disappears* under a recency-ordered greedy token-fill. Measured directly on the R19 labels, using the query for nothing: recency-only newest-40 scores **5.47%** evidence-turn recall (92.5% zero-evidence, 1,171 tok) against BM25 top-40's **59.86%** (24.9% zero, 2,841 tok) — **~11x**. Even at 1.7x the budget (newest 160, 4,747 tok) recency reaches only 24.67%. The corpus explains it: **602 turns per question on average**, so 40 turns is 6.6% of it and LoCoMo scatters evidence by construction. **Half two — changing the *unit* — is NOT tested here** and the probe uses raw turns, so this does not refute typed slots; but that half requires write-time LLM extraction, which is independently measured destructive (a frontier model failed **54% of problems it had previously solved** consolidating even from ground truth) and produces the representation our one validated lever depends on avoiding (verbatim+timestamps 50.2% vs extracted 31.2% vs summarised 7.5% on temporal; R11's +14.2pp came from bare dates on verbatim turns). It also contradicts the zero-inference property outright. Verdict: read path refuted, unit half disfavoured on a hostile prior, axis not closed — but no longer the obvious unexplored direction, since R19 showed real headroom remains inside the retrieval axis we already have. | 2026-08-08 | [result](internal/g5-typed-units-probe-2026-08-08.md) |
| **Outcome-ledger statistics (G3)** | **ESTIMATORS SHIPPED; the correction they exist for is BLOCKED ON DATA, and the shortfall is now a number.** The flat nudge (`signal_score = MIN(signal_score + strength, 1.0)`) has three compounding defects: no denominator, no exposure correction, unbounded compounding. `spectral_graph::ledger_stats` implements all three repairs — Wilson lower bound (1/1 scores **0.207**, 90/100 scores **0.825**), `log10(1+used)` saturation, and a position-bias divide-out — with 10 unit tests, all pure and offline. **The position-bias curve cannot be estimated:** the live ledger holds **160 adjudicated deliveries, 11 used, across 40 ranks = 4.0 per rank**. Detecting even a coarse 25%-vs-10% rank bias at 95%/80% needs **100/rank = 4,000 total, 25x** what exists (62x for moderate, 226x for subtle). `unreported` is 600 vs 160 adjudicated, so the nearest source is adjudicating what is already collected. Unobserved ranks return **None, not 0.0** — conflating never-observed with never-used is R15's error class. **Wilson alone is usable today** (every currently-reinforced memory rests on a *single* use and receives the increment a 90/100 memory would get) but is a default-ranking change on the consumer's live brain, so it is NEEDS-PREREG and not defaulted on. The §G3 claim that the co-retrieval regression is position bias **remains an untested hypothesis** — it predicts exactly the curve the data cannot estimate. | 2026-08-08 | [result](internal/g3-ledger-stats-2026-08-08.md) |
| **Cascade fetch_mult=3 for preference evidence (stage 0)** | **STOP — preregistered null, no paid run.** Stage-0 screen on the R15 evidence-turn endpoint, 500 LongMemEval questions, reused brains, single variable `SPECTRAL_CASCADE_FETCH_MULT=3`. Both preconditions passed first (pre-arm reproduced the archived post-R16 arm **0/500** and itself **0/500**). Result: `single-session-preference` is **bit-identical** — 29/44 evidence turns, 9 zero-evidence, **+0.0pp** against a **>=+10.0pp** threshold, and 9 zero-evidence against a **<=6** threshold. Two of four pinned criteria fail. The lever is not weak, it is **inert** for this category (it did change 223/500 contexts overall, +3 evidence turns, so the variable was live). Converts the inconclusive 2026-07-14 actor A/B into a decided retrieval result; default stays 1. **Diagnostic worth more than the lever:** sweeping k on the same brains, preference evidence-turn recall goes 29/44 at k=40 to **44/44 at k=200** — every evidence turn is in the FTS match set, so the entire **34.1pp gap is ranking, not vocabulary**, with a measured ceiling of 100% over the current match set. Contrast `g4-proximity`, where 88.8% of below-cut LoCoMo evidence was lexically thin: **the two gaps do not share a fix.** Preference is also the cheapest category (9,076 vs 14,213 mean tokens), making shape-conditional k a natural next prereg — deliberately NOT tried, since a failed gate does not license improvisation. | 2026-08-08 | [prereg](internal/preference-evidence-retrieval-prereg-2026-08-07.md), [result](internal/preference-stage0-result-2026-08-08.md) |
| **Term proximity (G4)** | **REFUTED, with a mechanism — $0, 250 LoCoMo questions on R19 turn labels.** The last classic IR signal never tested here. Proximity separates evidence from distractors **22.6x** in isolation (mean 0.1197 vs 0.0053; 91.8% of non-evidence score exactly 0) — and buys **nothing**. Baseline ev-turn recall 72.7% macro / 231-356 micro; best arm across a full sweep (fetch-mult 3 and 12, weights 0.005 to 16) is +1 evidence turn, and large weights degrade **monotonically** to 61.5%. Two measurements explain it: (a) **redundant** — proximity-positive candidates have mean BM25 rank 54.6 vs 192.5 overall and **51.6% are already inside the top 40**, so it re-states a preference `Σ IDF` already encodes; (b) **blind to the target** — of the 80 evidence turns ranked below the k=40 cut, **88.8% score proximity 0** because they contain at most one query term. The deep misses are lexically thin, not badly positioned, so no positional signal can reach them. Capability shipped **default off** on both topk and cascade; the reusable part is `apply_reranking_pipeline_with_query`, an additive query-conditioned entry point for a pipeline that was deliberately query-free. Points the next lever at coverage/vocabulary bridging (expansion, never tested) rather than rerank (closed). | 2026-08-08 | [result](internal/g4-proximity-result-2026-08-08.md) |
| **K=60→80 admission widening** | **REJECTED** — +36.5% context tokens buys zero new answer sessions and zero case unblocks; pure redundant-evidence spend. | 2026-07-20 | [k-admission-test](internal/k-admission-test-2026-07-20.md) |
| **RERANK associative spreading (weak actor)** | **REFUTED** — powered A/B on the full knowledge-update category: 18/78 vs 20/78, McNemar p=0.81; the encouraging n=30 pilot (+10pp) was regression to the mean (held-out split net −1). Retrieval-lever family closed with a measured null. | 2026-07-27 | [local-actor-ab-result](internal/local-actor-ab-result-2026-07-27.md) |
| **Assistant-turn cap (0.36)** | **REJECTED** — −53% context tokens but accuracy 85.0% → 70.0% (−15pp) on the SSA-weighted sample; all 7 regressions were single-session-assistant, exactly where the evidence lives in truncated turns. | 2026-07-02 | [TIER1_RESULTS](internal/TIER1_RESULTS.md) |
| **Spectrogram as a recall path** | **RETIRED** — enabling write-time spectrograms changed 0/500 retrieval contexts; the crate is repointed to the recognition lineage's history, not recall. | 2026-07-02 | [ORACLE_TIER0](internal/ORACLE_TIER0.md), [SPECTROGRAM_AUDIT](internal/SPECTROGRAM_AUDIT.md) |
| **Recognition accuracy claim** | **WITHDRAWN** — the peak-pair engine is the most accurate method in no regime (MinHash 0.998 vs engine 0.941 on lexical re-encounter; embeddings 0.703 vs 0.543 on semantic). Its measured differentiator is auditable verdicts, not accuracy. | 2026-07-03 | [RECOGNITION_BASELINE](internal/RECOGNITION_BASELINE.md) |
| **Ingest gap decomposition** | **Durability is NOT the bottleneck** — batched SQLite+FTS5 runs **60,489 ev/s, 2.7x FASTER than MinHash+BM25**. Spectral's remaining 7.2x gap is 5% insert floor, **21% per-event transaction commit**, **73% Spectral's own per-event work** (classify/score/hash/episode), which has never been profiled at that granularity. Batching buys 7.2x → ~5.7x. | 2026-08-03 | [decomposition](internal/ingest-gap-decomposition-2026-08-03.md) |
| **Phase 0 vs MinHash+BM25 — RE-RUN 2026-08-03** | **Verdict moved.** The recorded 43 ev/s was stale (the `max_fingerprint_peers` cap landed after it and was never re-measured): the unmodified config now runs **428 ev/s**, and with fingerprints retired **3,148 ev/s at 5.07 KB/event**. Gap to MinHash+BM25 goes from **~500x/40–70x** to **7.2x/2.4x**, determinism tied at 1.0. The storage comparison is RAM-only index vs durable signed store. Cost-moat conclusion unchanged and still negative. | 2026-08-03 | [re-run](internal/phase0-rerun-2026-08-03.md) |
| **Phase 0 vs MinHash+BM25 (original, SUPERSEDED)** | **LOSES the systems axes** — MinHash+BM25 is also $0/offline/deterministic and is ~500× faster to ingest and ~40–70× lighter; the cost moat vs an embedding stack is ~$0.04/month at real volume, below the pre-registered $5 kill line. Spectral's remaining edges: auditability, verified deletion, graph, federation. | 2026-07-03 | [PHASE0_RESULTS](internal/PHASE0_RESULTS.md) |
| **Two-stage actor synthesis (per-session extract + aggregate)** | **STOPPED** — neither target case flipped; the extraction step itself is non-deterministic (same prompt, same model, different output), so net lift is not reproducible. | 2026-05-14 | [candidate-c-aggregation-v2](internal/candidate-c-aggregation-v2.md) |
| **Descriptions in the actor context** | **NO LIFT** — 9/10 vs 9/10, +7% tokens/+11% cost; a gloss is redundant when the memory is retrieved and useless when it isn't. Descriptions stay retrieval-side. | 2026-07-11 | [descriptions-in-actor-context](internal/descriptions-in-actor-context-2026-07-11.md) |
| **ACR retrieval lift → accuracy** | **DOES NOT CONVERT** — +18–40pp *answer-session turn coverage* on all six memory types (published as "answer-key recall"; it is the ~12x-diluted metric R15 names, and the evidence-turn delta for this lever was never computed — the arm row files are not retained, so it is **unknown**), but weak-actor accuracy went 57/74 → 55/74 (net −2): the extra evidence distracts more than it fixes. | 2026-07-15 | [acr-lift-all-memory-types](internal/acr-lift-all-memory-types-2026-07-15.md) |
| **Novelty folded into signal score** | **REJECTED** — measured all-downside; novelty and durability are orthogonal axes, keep them separate. | 2026-07-14 | [novelty-signal-lever](internal/novelty-signal-lever-measured-2026-07-14.md) |
| **Query-conditioned answerability rerank** | **REFUTED over 3 runs and 3 mechanisms** — reorder-only −0.1 rank1 (sign test p=0.059); membership-changing +0.1pp; both, on a fixed foundation, **+0.3pp session-recall / +0.3pp answer-session turn coverage** against a preregistered +1.0pp gate. **Metric caveat (R15, 2026-08-07):** the second gate criterion was the ~12x-diluted coverage metric, not evidence-turn recall; the arm row files were not retained, so what the correct metric would have shown here is **unknown**. The verdict is not restated on that basis. Effect size tracked the foundation fixes exactly (+0.1 → +0.3pp) so the lever was fully active, not broken. Closes the *query-conditioned* family — the one kind never previously tested, and the deterministic analogue of the best-measured lever (cross-encoder). | 2026-08-02 | [prereg](internal/answerability-prereg-2026-08-02.md), [run 1](internal/answerability-result-run1-2026-08-02.md), [run 2](internal/answerability-result-run2-2026-08-02.md), [run 3](internal/answerability-result-run3-2026-08-02.md) |
| **TACT fingerprint tier as recall** | **NO HEADROOM** — content recognition never beats FTS at recall (0 wins, 2 losses, 9 ties); both are lexical and BM25 is already strong. | 2026-07-15 | [tact-unlock-synthesis](internal/tact-unlock-synthesis-2026-07-15.md) |

| **Read-time supersession suppression (regex over free text)** | **REJECTED on precision** — conservative `my <attr> is …` extraction fires on 1.02% of 246,930 turns and makes only 8.2% of questions suppressible, but the matches are dominated by **assistant boilerplate** ("My knowledge is derived from…", "As an AI language model…"), not user facts. Supersession belongs in the typed graph layer, where Spectral already implements it correctly. | 2026-08-03 | [prereg](internal/supersession-prereg-2026-08-03.md), [result](internal/supersession-result-2026-08-03.md) |
| **Wall-clock decay breaks reproducibility** | **CLAIM REFUTED (my own)** — recall ordering is time-invariant on both paths: `apply_recency_weight` is multiplicative so a clock shift scales all scores by a common factor, and `recall_at` never re-sorts after decaying. Measured at anchors +200d/+700d/+5y/+30y: zero ordering changes. Decayed *score values* do move. Latent defect found: `recall_at` computes a decayed score it never uses for ordering. | 2026-08-03 | [record](internal/decay-time-invariance-2026-08-03.md) |
| **Policy V2Fixed (classifier defect repair)** | **INCONCLUSIVE (n=1)** — gates passed on paper (preference session-recall 93.3%→96.7%, control moved 0.0pp) but the entire effect is **one question**; the +2.0pp gate was miscalibrated for n=30, where one question *is* 3.3pp. Default stays V1. Two preregistered predictions confirmed. | 2026-08-02 | [prereg](internal/policy-v2-prereg-2026-08-02.md), [result](internal/policy-v2-result-2026-08-02.md) |
| **`*CurrentState` sub-shapes** | **DEAD WEIGHT — confirmed** — `FactualCurrentState`/`CountingCurrentState` share a cascade profile and route with their base shape, so reclassification changes a label and nothing else (2 questions reclassified, zero retrieval effect). Current-state handling is missing from the per-shape **profile table**, not the classifier. | 2026-08-02 | [result](internal/policy-v2-result-2026-08-02.md) |
| **"Fix preference routing to fix the weakest category"** | **REFUTED** — `single-session-preference` is 56.0% end-to-end but its **session-recall is already 93.3%**. The evidence is retrieved for 28/30 questions and the actor still misses 44%. ~37pp of the gap is actor-side; total retrieval headroom is 6.7pp. | 2026-08-02 | [result](internal/policy-v2-result-2026-08-02.md) |

## Measured, not shipped (gated on a value decision)

| Experiment | Verdict | Date | Record |
|---|---|---|---|
| **Cross-encoder rerank** | Best retrieval-precision lever measured to date (+1.6pp session recall on the hard set, zero session losses) — but unproven to end-to-end accuracy and requires a neural model, colliding with the no-embedding stance. Filed, not shipped. | 2026-07-21 | [cross-encoder-dense-rerank](internal/cross-encoder-dense-rerank-2026-07-21.md) |
| **Dense (bi-encoder) hybrid** | Near-null fused over the BM25-won lexical pool, but the only measured lever that reaches the vocabulary-mismatch floor (lifts a pos-302 operand to rank 19). Shipping it is a value decision against the no-embedding stance, not a benchmark verdict. | 2026-07-21 | [cross-encoder-dense-rerank](internal/cross-encoder-dense-rerank-2026-07-21.md) |
| **Cascade fetch-pool widening (`fetch_mult`)** | Capability shipped, default OFF — Pareto-safe on the retrieval metric but a proven accuracy no-op; do not re-default without a powered actor A/B. | 2026-07-14 | [cascade-fetch-mult-lever](internal/cascade-fetch-mult-lever-2026-07-14.md) |

## External evidence: the benchmark leader does no ranking

Mastra's Observational Memory holds the LongMemEval state of the art (**84.23%**
gpt-4o, **94.87%** gpt-5-mini) using **static retrieval** — a compressed
observation log in the prompt prefix, with *no query-based retrieval and no
per-turn reranking at all*. It buys accuracy with two background LLM agents at
write time (Observer, Reflector), the mechanism this project's thesis rejects.

This is independent confirmation of the pattern below: LongMemEval accuracy is
not in retrieval ranking. See
[source-survey-2026-08-03](internal/source-survey-2026-08-03.md).

## Structural findings (why some levers could not have worked)

Two defects found while instrumenting the answerability lever, both verified in
code. They constrain how any *future* retrieval experiment must be built.

| Finding | Consequence |
|---|---|
| **Session-grouped rendering discards rank order.** `format_hits_grouped_capped_dated` groups by `episode_id`, sorts within a group by `key`, and orders groups by date. Rank is never consulted. | On the cascade route — the default for every non-Temporal shape, 70% of the held-out set — a **rerank-only lever cannot change what the actor sees at all**. Only admission (set membership) survives rendering. |
| **Harness pool widening is a no-op on cascade.** `run_cascade_pipeline_scoped` ends with `results.truncate(config.k)` (`cascade_layers.rs:441`), so `merged_hits.take(k * widen)` returns the same `k` items. | `ACTR_POOL_WIDEN` is affected identically, so **ACT-R's pool widening is inert on the cascade route**. ACT-R there is doubly inert: it cannot change membership, and its reordering never reaches the actor. ACT-R is an off-by-default env lever and no published number depends on it, but its recorded behaviour should not be trusted without a re-run. |

Together these give a partial *mechanistic* explanation for the pattern below,
and narrow it: on the default route, rerank-shaped levers were never able to
convert to accuracy, because their effect was erased before the actor saw
anything. Admission levers (K widening, fetch-mult, spreading) were measured on
their merits and rejected on their merits.

**Both are now fixed**, defaulting to the previous behaviour and verified
byte-identical with all levers off (oracle, 0/120 diffs):

- widening reaches `pipeline_config.k` before the pipeline runs, so an
  admission lever changes membership on the cascade route — measured to take a
  lever from acting on 36/120 questions to 114/120;
- `render::SessionOrder::ByRank` orders sessions by best contained rank, so a
  rerank reaches the actor — measured to change the rendered context on 84/84
  cascade questions while changing the retrieved set on 0.

Note the second is **unmeasurable on the $0 oracle**: its metrics derive from
`retrieved_keys`, so rendered session order is invisible to them by
construction. It removes a structural blocker; it is not itself evidence of
benefit.

The pattern across the record: retrieval recall is near ceiling and retrieval
levers stopped converting to accuracy long ago — the actor/synthesis stage is
the ceiling ([TIER1_PORTER_WIDEN](internal/TIER1_PORTER_WIDEN.md),
[local-actor-ab-result](internal/local-actor-ab-result-2026-07-27.md)). The
levers that held are the cheap deterministic ones (porter) and the actor-side
prompt fixes (identity-keyed counting).
