# PR Triage: 2026-05-14

**Date**: 2026-05-14
**Branch**: `main` (triage doc committed directly)

---

## Summary

| PR | Title | Action | Reasoning |
|----|-------|--------|-----------|
| #49 | tokio 1.52.1 → 1.52.3 | **Merged** | Patch update, CI green, sync fixes only |
| #95 | Category audit + path-to-90% | **Merged** | Docs only, CI green, no conflicts |
| #96 | External research synthesis | **Merged** | Docs only, CI green, no conflicts (was draft, marked ready) |
| #50 | indicatif 0.17 → 0.18 | **Flag for human** | Semver-breaking bump (0.17→0.18), CI green |
| #51 | rand 0.8 → 0.9 | **Flag for human** | Major version bump, CI failing (build + test + lint) |
| #92 | Multi-session + preference failure triage | **Recommend close** | Superseded by PRs #93, #99, #100 |
| #80 | Co-retrieval signal + bench warm-up | **Recommend close** | Core feature landed via PR #90; bench evolved since |
| #88 | Cascade max_confidence investigation | **Flag for human** | Mixed: code already on main, doc unique, CI failing |
| #82 | Spectrograms + preflight (Phase 2a) | **Flag for human** | Feature PR with code changes, needs human review |

**Totals**: 3 merged, 4 flagged for human, 2 recommended-close.

---

## Per-PR Assessment

### #49 -- tokio 1.52.1 → 1.52.3 (dependabot) -- MERGED

- **Contains**: Cargo.lock only (patch bump)
- **CI**: All 4 checks green (test ubuntu, test macos, lint, build)
- **Version type**: Patch (1.52.1 → 1.52.3). Fixes: mpsc channel `len()` underflow, `OwnedPermit::release()` notification, `RwLock` max_readers check
- **Conflicts**: None (Cargo.lock auto-resolved by squash merge)
- **Action**: Merged via `gh pr merge --squash --delete-branch`

### #95 -- Category audit + path-to-90% reassessment -- MERGED

- **Contains**: 1 new file (`docs/internal/category-audit-2026-05-12.md`, 228 lines)
- **CI**: All 4 checks green
- **State**: Open (not draft)
- **Conflicts**: None -- file doesn't exist on main
- **Content check**: No contradiction with item #11 or #12 investigation docs. The category audit references items #8, #11, #12, #19, #20 as future work; items #11 and #12 have since been investigated and deferred, which is consistent (the audit's ceiling estimates assumed those items might ship)
- **Action**: Merged via `gh pr merge --squash --delete-branch`

### #96 -- External research synthesis -- MERGED

- **Contains**: 1 new file (`docs/internal/external-research-synthesis-2026-05-12.md`, 392 lines)
- **CI**: All 4 checks green
- **State**: Was DRAFT -- marked ready, then merged
- **Conflicts**: None -- file doesn't exist on main
- **Content check**: References quote-first extraction (shipped as PR #97), contextual BM25 (item #8, shipped as PR #104). No contradictions -- the synthesis correctly identified these as high-leverage and both have since landed
- **Action**: Marked ready via `gh pr ready`, merged via `gh pr merge --squash --delete-branch`

### #50 -- indicatif 0.17.11 → 0.18.4 (dependabot) -- FLAG FOR HUMAN

- **Contains**: Cargo.lock + `crates/spectral-bench-accuracy/Cargo.toml` (version bump)
- **CI**: All 4 checks green
- **Version type**: **Semver-breaking** in Rust 0.x semantics. 0.17→0.18 is a major change. The indicatif changelog confirms: "0.17.12 had to be yanked because the console upgrade was a semver-incompatible change. Rerelease as 0.18.0 instead."
- **Risk**: indicatif is used only in `spectral-bench-accuracy` (progress bars). CI passes, so the API surface we use is compatible. Low blast radius.
- **Question for human**: CI is green and the crate is only used in the bench harness. Merge despite major version bump? The dependency is non-critical (progress display only).

### #51 -- rand 0.8.6 → 0.9.4 (dependabot) -- FLAG FOR HUMAN

- **Contains**: Cargo.lock + Cargo.toml (workspace-level rand version)
- **CI**: **FAILING** -- build, lint, and test all fail. macOS test failure, ubuntu cancelled
- **Version type**: **Major version bump** (0.8 → 0.9). Breaking API changes: `rand_core` v0.9, new feature defaults (`small_rng`), distribution module restructured
- **Risk**: High. rand is used in `spectral-bench-accuracy` for reproducible randomization. API breaks confirmed by CI failure
- **Question for human**: Close this PR and let dependabot re-create when the codebase is ready for the rand 0.9 migration? Or hold for manual migration work?

### #92 -- Multi-session + preference failure triage -- RECOMMEND CLOSE

- **Contains**: 1 new file (`docs/internal/multi-session-failure-triage-2026-05-12.md`, 94 lines)
- **CI**: All 4 checks green
- **State**: DRAFT
- **Superseded by**: Three later, more detailed analyses that are already on main:
  - PR #93 (`multi-session-failure-investigation.md`) -- full walkthrough of the same 10 failures with the "embedded-reference-in-different-primary-context" diagnosis
  - PR #99 (`multi-session-failure-classification-2026-05-12.md`) -- refined classification: DEFINITION_DISAGREEMENT (3) + GENUINE_MISS (4→2) + AMBIGUOUS (2) + RETRIEVAL_MISS (2) + TEMPORAL (1)
  - PR #100 (`pr-99-recommendations-verification.md`) -- verified PR #99 estimates, added reasoning-aware judge analysis
- **Content relationship**: #92 was the first pass. It found 9/10 ACTOR_MISS, 1 RETRIEVAL_MISS. PR #93 expanded this into a full investigation with walkthroughs. PR #99 reclassified with finer taxonomy. #92's content is a strict subset of what's on main
- **Recommendation**: Close without merging. Adding it would create a third multi-session failure doc that's less detailed than the two already on main

### #80 -- Co-retrieval signal in cascade + bench warm-up -- RECOMMEND CLOSE

- **Contains**: Code changes to ranking.rs (co-retrieval boost signal), brain.rs (Box→Arc for memory_store), cascade_layers.rs, bench eval.rs/main.rs/retrieval.rs (warm-up queries + --enable-co-retrieval flag)
- **CI**: All 4 checks green (on its own branch)
- **State**: DRAFT
- **Superseded by**: PR #90 (`d7fd76c feat(ranking): co-retrieval signal in cascade ranking (#90)`) which landed the co-retrieval ranking signal on main via a different implementation. Key differences:
  - PR #90 uses `compute_co_retrieval_boosts()` as a pre-computed map passed to `apply_reranking_pipeline()` -- cleaner than #80's inline async bridging
  - PR #90 does NOT include the bench warm-up code (`--warmup-queries`, `--enable-co-retrieval` flags)
  - PR #80's `Box→Arc` change for `memory_store` was NOT adopted -- main still uses `Box<dyn MemoryStore>`
- **Warm-up code**: The warm-up feature (generate queries from haystack, run cascade to populate retrieval_events, rebuild co_retrieval_index) is unique to #80 and hasn't landed. However, the bench harness has evolved substantially since May 10 (shape-routing, quote-first extraction, max_tokens changes). The warm-up code would need significant rework
- **Recommendation**: Close. Core feature landed differently via #90. Warm-up concept is interesting but would need reimplementation on current main. Note the warm-up idea in the close comment for future reference

### #88 -- Cascade max_confidence=0.85 plateau investigation -- FLAG FOR HUMAN

- **Contains**: Mixed content across 2 commits:
  1. **Commit 1** (`46823386`): Shape-routed actor strategies -- code changes to actor.rs, eval.rs, main.rs, retrieval.rs, 8 prompt templates, report.rs. This is the PR #86 implementation
  2. **Commit 2** (`b159bceb`): Cascade max_confidence investigation doc (`docs/internal/cascade-max-confidence-investigation.md`)
- **CI**: **FAILING** -- macOS test failure, ubuntu cancelled. Build and lint pass
- **State**: DRAFT
- **Superseded content**: Commit 1 (shape-routed actors) already landed on main via PR #86 (`d86cbbe`). The `shape-routed-actors-proposal.md` doc is also already on main
- **Unique content**: Commit 2 (cascade investigation doc) has NOT landed on main. It documents the `.min(0.85)` bug in `brain.rs:1189-1192`, confirming that all 166 cascade runs produce identical `max_confidence=0.85`. This is backlog item #19
- **Question for human**: The investigation doc is valuable (it's item #19's analysis). Options:
  - (a) Extract the doc into a clean single-commit PR on current main
  - (b) Cherry-pick commit 2 onto main
  - (c) Close this PR and note the finding is captured in backlog item #19's description
  - The code in commit 1 should NOT be merged (already on main via #86)

### #82 -- Enable spectrograms + preflight spot-check (Phase 2a) -- FLAG FOR HUMAN

- **Contains**: Code changes (5 commits):
  - `enable_spectrogram: true` in bench ingest
  - `preflight` subcommand with `--all` flag (SQL spot-checks: wing/hall distribution, spectrogram coverage, fingerprint time_delta_bucket)
  - rusqlite dependency added to spectral-bench-accuracy
  - 2 new tests (ingest_computes_spectrograms, ingest_populates_wings_and_halls)
  - 5 new docs (phase2a-result, phase2a-bench-runbook, phase2a-cost-estimate, permagent-ingest-contract, librarian-queue-design)
- **CI**: All 4 checks green
- **State**: Open (not draft)
- **Conflicts**: Likely -- bench code has changed significantly since May 11 (PRs #86, #90, #91, #94, #97, #98, #104, #105). Cargo.lock will conflict at minimum. Code files (ingest.rs, main.rs) have been modified by later PRs
- **Not superseded**: Spectrogram enablement and preflight subcommand are unique; no later PR replaces this functionality. The permagent-ingest-contract.md and librarian-queue-design.md are forward-looking design docs that haven't been superseded
- **Question for human**: This PR is 3+ days behind main with likely conflicts in bench code. Options:
  - (a) Rebase onto current main and re-run CI (estimated effort: 1-2h for conflict resolution)
  - (b) Close and re-implement spectrogram enablement + preflight as a fresh PR on current main (simpler if only a subset of changes are still desired)
  - (c) Cherry-pick the docs (5 files) separately if the design docs are still relevant
  - Note: the phase2a bench procedure described in the PR body may no longer apply given the bench evolution since May 11

---

## Actions Taken

1. **Merged #49** (tokio 1.52.1→1.52.3) -- squash merge, branch deleted
2. **Merged #95** (category audit doc) -- squash merge, branch deleted
3. **Merged #96** (external research synthesis) -- marked ready from draft, squash merge, branch deleted

## Pending Human Decisions

| PR | Decision needed | Urgency |
|----|----------------|---------|
| #50 (indicatif) | Merge despite 0.17→0.18 breaking change? CI green, bench-only dep | Low |
| #51 (rand) | Close or hold for migration? CI failing, major API changes | Low |
| #88 (cascade investigation) | Extract investigation doc to clean PR? Code already on main | Low |
| #82 (spectrograms + preflight) | Rebase or reimplement? Likely conflicts with current main | Medium |

## Recommended Closes

| PR | Superseded by | Note |
|----|--------------|------|
| #92 (multi-session triage) | PRs #93, #99, #100 | Earlier, less-detailed pass at same analysis |
| #80 (co-retrieval signal) | PR #90 | Core feature landed differently; warm-up concept worth preserving as future backlog note |
