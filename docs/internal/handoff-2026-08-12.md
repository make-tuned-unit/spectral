# Handoff — 2026-08-12 (machine restarted overnight mid-R31)

For the next session (Claude on the other mac mini): read this first, then the R31
prereg at `docs/internal/adjacency-accuracy-replication-prereg-2026-08-11.md`.

## State

- **Repo: clean.** Everything through `6cfce97` (MEASURED_RECORD rows for R29/R30)
  was committed and pushed before the restart. Nothing was lost from git.
- **R31 run: interrupted.** `scripts/run_accuracy_replication.sh` launched arm `b0`
  at 21:13 on 2026-08-11 and the machine restarted at ~21:14. No arm produced a
  report — neither `b0.json` nor `b_adj.json` exists. The only residue is
  `~/spectral-local-bench/accuracy-repl-2026-08-11/work-b0/` (a checkpoint.json
  ~1 conversation in). That directory lives outside the repo on the OLD machine
  and does not travel with this push.

## What R31 is (short version)

R30's conversion null came from an instrument that couldn't see (11.79% baseline
on its floor). R31 re-asks the conversion question on single-session-user, where
the baseline is far off the floor and adjacency's retrieval gain was verified
equivalent before the slice was chosen. Sample of 300 IDs is committed at
`docs/internal/r31-sample-ids.txt` (seed 20260811). This is the declared LAST
slice — if null, the conversion question closes at $0.

## To resume R31

1. Prereqs on whichever machine runs it:
   - `cargo build --release -p spectral-bench-accuracy`
   - `~/spectral-local-bench/locomo_full_answerable_labelled.json` (dataset)
   - Ollama serving `qwen25-16k` on `localhost:11434` ($0, on-device — no key needed)
2. Run `scripts/run_accuracy_replication.sh`. It is idempotent: it skips any arm
   whose `<label>.json` already exists and is non-empty, so re-running from
   scratch is safe. Both arms (`b0`, then `b_adj` with `SPECTRAL_ADJACENCY=1`)
   must reach all 300 questions.
3. If resuming on the old machine, the stale `work-b0/` dir can be left in place
   (the binary checkpoints per work-dir) or deleted for a clean start — either is
   fine; the report only gets written at completion.
4. Score per the prereg: all three metrics registered up front, multiplicity
   correction as declared, floor bar 20%, ceiling bar as written. Then write
   `docs/internal/adjacency-accuracy-replication-result-2026-08-11.md` and a
   MEASURED_RECORD row, whichever way it lands.

## Standing reminders

- ROTATE THE BENCH KEY (still outstanding from 2026-08-10; not needed for R31
  itself since it runs on Ollama, but rotate before any paid run).
- No more paid runs without explicit budget sign-off; always full N.
